# ===========================================
# Codeck - Environment Variables
# ===========================================
# Copy this file to .env and fill in the required values
# IMPORTANT: Never commit .env to git (covered by .gitignore)
# Set file permissions to 0600 on host: chmod 600 .env

# Webapp port (default: 80)
CODECK_PORT=80

# GitHub Token (optional — for cloning private repos)
# SECURITY: Use fine-grained personal access tokens with repository-scoped permissions
# Create at: https://github.com/settings/tokens?type=beta
# Regenerate periodically (monthly for production, as-needed for personal use)
GITHUB_TOKEN=

# Anthropic API Key (optional — alternative to OAuth login)
# SECURITY: Prefer OAuth login (automatic token refresh, encryption at rest)
# If using API key, ensure .env has 0600 permissions on host
ANTHROPIC_API_KEY=

# Encryption key for Claude OAuth tokens (recommended for production)
# Generate with: openssl rand -hex 32
# NEVER commit this to git! If leaked, all encrypted tokens can be decrypted.
# If not set, a hostname-based key is derived (less secure for team deployments)
CODECK_ENCRYPTION_KEY=
